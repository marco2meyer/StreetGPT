http://{$SITE_HOST} {
	redir https://{$SITE_HOST}{uri} permanent
}

{$SITE_HOST} {
	tls {
		issuer acme {
			dir https://acme-v02.api.letsencrypt.org/directory
		}
	}

	encode zstd gzip
	handle /return-bridge.html {
		root * /srv/caddy-static
		file_server
	}
	reverse_proxy app:8501
}
